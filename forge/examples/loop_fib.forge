#include "lib/std.forge"

fn fib(n: i64) -> i64:
    var a: i64 = 0
    var b: i64 = 1
    var i: i64 = 0
    while i < n:
        var t: i64 = a + b
        a = b
        b = t
        i = i + 1
    return a

fn main():
    var r: i64 = fib(10)
    sys_exit(r)
